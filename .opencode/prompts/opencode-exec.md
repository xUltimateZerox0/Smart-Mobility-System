[FASE DI EXECUTION: SCAFFOLDING]
In allegato trovi il documento master-spec.md.
Il "Clarity Gate" è superato. Il tuo contesto è ora blindato sulle regole architetturali precedentemente acquisite.

ORDINE ESECUTIVO:

    Inizia immediatamente l'esecuzione e genera lo scheletro del software seguendo rigorosamente l'ordine definito nella sezione <EXECUTION_PLAN> del tuo system prompt.

    Assicurati che ogni file generato rispetti il formato di output <FILE_OUTPUT_FORMAT> per consentire il nostro parsing automatico.

    Rispetta in modo assoluto il <CRITICAL INJECTION PROTOCOL>: obbligo di @RequiredArgsConstructor e campi private final per la Dependency Injection. Zero difetti di compilazione.

    Non implementare la logica di business: usa le eccezioni Not Implemented come pattuito.

    Al termine di tutti i file Java/YML/XML, genera il blocco bash <PIPELINE_TRIGGER> per l'analisi SonarQube automatizzata.

Procedi ora con la generazione completa e deterministica.